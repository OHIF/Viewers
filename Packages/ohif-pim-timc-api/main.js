import { User } from './user';
import { Token } from './token';
import { Connector } from './connector';

export const PIMTIMCServerAPI = {
    User: User,
    Token: Token,
    Connector: Connector
};
