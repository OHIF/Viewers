@import "{design}/app.styl"

@keyframes zoomIn
  from
    transform(scale(0))
  to
    transform(scale(1))

@keyframes fadeIn
  from
    opacity: 0
  to
    opacity: 1

@keyframes fadeOut
  from
    opacity: 1
  to
    opacity: 0
