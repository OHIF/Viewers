# Copy this file to .env next to docker-compose.yml and fill in strong,
# unique values. docker compose refuses to start while any of these is unset.
KC_DB_PASSWORD=
KEYCLOAK_ADMIN_PASSWORD=
POSTGRES_PASSWORD=
