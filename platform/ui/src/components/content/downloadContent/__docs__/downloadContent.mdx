---
name: Download Content
menu: Components
route: /components/download-content
---

import { Playground, Props } from 'docz'
import { State } from 'react-powerplug'
import { DownloadContent } from './../index'
import NameSpace from '../../../__docs__/NameSpace'

# Download Dialog

## Basic usage

<Playground>
  <State
    initial={{}}
  >
    {({ state, setState }) => (
      <React.Fragment>
        <div>
          <pre>{JSON.stringify(state, null, 2)}</pre>
        </div>
        <div style={{ maxWidth: '400px', margin: '0 auto' }}>
          <DownloadContent />
        </div>
      </React.Fragment>
    )}
  </State>
</Playground>

## API

<Props of={DownloadContent} />

## Translation Namespace

<NameSpace name="DownloadContent" />
