---
name: InputSelect
menu: Components
route: components/InputSelect
---

import { useState } from 'react';
import { Playground, Props } from 'docz';
import InputSelect from './';

# Filter Select

## Import

```javascript
import { InputSelect } from '@ohif/ui';
```

## Basic usage

<Playground>
  {() => {
    const [values, setValues] = useState([]);
    return (
      <div className="flex flex-col flex-1 p-4 items-center">
        <div className="w-56">
          <InputSelect
            label="Modality"
            options={[
              { value: 'SEG', label: 'SEG' },
              { value: 'CT', label: 'CT' },
              { value: 'MR', label: 'MR' },
              { value: 'SR', label: 'SR' },
            ]}
            value={values}
            onChange={values => {
              setValues(values);
            }}
          />
        </div>
      </div>
    );
  }}
</Playground>

## Properties

<Props of={InputSelect} />
