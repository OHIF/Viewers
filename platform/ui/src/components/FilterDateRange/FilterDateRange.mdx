---
name: FilterDateRange
menu: Components
route: components/filterDateRange
---

import { useState } from 'react';
import { Playground, Props } from 'docz';
import FilterDateRange from './';

# Filter Date Range

## Import

```javascript
import { FilterDateRange } from '@ohif/ui';
```

## Basic usage

<Playground>
  {() => {
    const [dates, setDates] = useState({
      startDate: null,
      endDate: null,
    });
    return (
      <div className="p-4 flex flex-col items-center">
        <div className="w-56">
          <FilterDateRange
            label="Dates"
            inputValue={dates}
            onChange={dates => setDates(dates)}
          />
        </div>
      </div>
    );
  }}
</Playground>

## Properties

<Props of={FilterDateRange} />
