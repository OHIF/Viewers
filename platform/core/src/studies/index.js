import {
  sortStudy,
  sortStudySeries,
  sortStudyInstances,
  sortingCriteria,
} from './sortStudy';

const studies = {
  sortStudy,
  sortStudySeries,
  sortStudyInstances,
  sortingCriteria,
};

export default studies;
