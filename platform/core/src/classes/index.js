import CommandsManager from './CommandsManager';
import HotkeysManager from './HotkeysManager';
import MouseBindingsManager from './MouseBindingsManager';
import ImageSet from './ImageSet';
import MetadataProvider from './MetadataProvider';

export { MetadataProvider, CommandsManager, HotkeysManager, MouseBindingsManager, ImageSet };

const classes = {
  MetadataProvider,
  CommandsManager,
  HotkeysManager,
  MouseBindingsManager,
  ImageSet,
};

export default classes;
