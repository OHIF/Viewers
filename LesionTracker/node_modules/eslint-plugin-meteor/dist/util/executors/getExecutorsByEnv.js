'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _set = require('babel-runtime/core-js/set');

var _set2 = _interopRequireDefault(_set);

exports.default = getExecutorsByEnv;

var _environment = require('../environment');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * Transforms an environment into executors
 * @param {ENVIRONMENT} env An Environment
 * @return {Set} A Set of executors
 */
function getExecutorsByEnv(env) {
  var executors = new _set2.default();
  switch (env) {
    case _environment.CLIENT:
    case _environment.COMPATIBILITY:
      executors.add('browser');
      executors.add('cordova');
      break;
    case _environment.SERVER:
      executors.add('server');
      break;
    case _environment.UNIVERSAL:
      executors.add('server');
      executors.add('browser');
      executors.add('cordova');
      break;
    case _environment.PACKAGE_CONFIG:
    case _environment.MOBILE_CONFIG:
    case _environment.PUBLIC:
    case _environment.PRIVATE:
    case _environment.TEST:
    case _environment.NODE_MODULE:
    case _environment.NON_METEOR:
    case _environment.PACKAGE:
    default:
      break;
  }
  return executors;
}