import { ViewportActionCorners } from '@ohif/ui-next';

export default {
  'ui.viewportActionCorner': ViewportActionCorners,
  // Number of action items to show per corner before collapsing
  'viewport.visibleActionItems': 2,
};
