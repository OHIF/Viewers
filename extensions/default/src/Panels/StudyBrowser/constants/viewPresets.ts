import type { viewPreset } from '../types/viewPreset';

const viewPresets = [
  {
    id: 'list',
    iconName: 'icon-list-view',
  },
  {
    id: 'thumbnails',
    iconName: 'icon-thumbnail-view',
  },
] as viewPreset[];

export { viewPresets };
