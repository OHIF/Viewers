import type { actionIcon } from '../types/actionsIcon';

const defaultActionIcons = [
  {
    id: 'settings',
    iconName: 'Filters',
    value: false,
  },
] as actionIcon[];

export { defaultActionIcons };
