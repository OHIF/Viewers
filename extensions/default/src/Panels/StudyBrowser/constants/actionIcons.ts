import type { actionIcon } from '../types/actionsIcon';

const actionIcons = [
  {
    id: 'settings',
    iconName: 'settings-bars',
    value: false,
  },
] as actionIcon[];

export { actionIcons };
