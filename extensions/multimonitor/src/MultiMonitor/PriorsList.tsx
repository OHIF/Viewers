import React, { useState, useEffect, useMemo } from 'react';

export default function PriorsList(props) {
  return (<div style={{ background: 'blue', color: 'green', width: '100%' }}>
    Hello Priors List
  </div>);
}
