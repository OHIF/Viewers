// Must match the npm package name so OHIF ExtensionManager registers this extension
// under the same key that modes reference it by (packageName.panelModule.elementName).
// All upstream OHIF extensions follow the same convention: id = packageJson.name.
export const id = '@blackvoxel/extension-blackvoxel-ai';
