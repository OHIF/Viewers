# Auto loading derived sequences Mode

## Introduction
Auto loading derived sequences mode allows you to automatically loads related derived sequences (SEG, SR, RTSTRUCT) when user opens an image series.
